import fermentableModal from '../../components/Modals/fermentableModal';
import { connect } from 'tls';

const mapStateToProps = (state) =>{

}
const mapDispatchToProps = dispatch => ({

})

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(fermentableModal)